# ===========================================
# Monad Mini App — copy to .env and fill in
# NEVER commit .env (it is in .gitignore)
# ===========================================

# App URL (required). Use your production URL or ngrok/cloudflared URL for dev.
# Example: https://your-app.vercel.app or https://abc123.ngrok.io
NEXT_PUBLIC_URL=""

# Optional: Farcaster / Neynar (for notifications, etc.)
# https://neynar.com
NEYNAR_API_KEY=""

# Optional: Upstash Redis (if using server-side features)
# https://upstash.com
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Reown AppKit (WalletConnect + connect modal). Get project ID: https://dashboard.reown.com
NEXT_PUBLIC_REOWN_PROJECT_ID=""

# Chain config (defaults to Monad Testnet if unset).
# Use a working RPC: https://testnet-rpc.monad.xyz (some providers e.g. Alchemy may return 500).
NEXT_PUBLIC_RPC_URL=""
NEXT_PUBLIC_CHAIN_ID=""
